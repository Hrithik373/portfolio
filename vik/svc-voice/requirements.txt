fastapi==0.115.6
uvicorn[standard]==0.32.1
prometheus-client==0.21.1
pydantic==2.10.3
python-multipart==0.0.17
# Phase 4 TODO: add openai-whisper (or faster-whisper) + a Piper/Coqui TTS
# runtime here once real voice I/O is wired — see app/models.py.
