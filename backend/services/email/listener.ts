/**
 * Hook for future IMAP / provider polling. Start any long-running inbound
 * listeners here when you add credentials.
 */
export const startEmailListener = () => {
  // no-op until an inbound transport is configured
}
