// Empty on purpose: this project doesn't use Tailwind/PostCSS plugins, but
// without a local config here, PostCSS walks up and picks up the parent
// portfolio repo's tailwind.config.js, which doesn't apply to this project.
export default {};
