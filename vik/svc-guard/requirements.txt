fastapi==0.115.6
uvicorn[standard]==0.32.1
prometheus-client==0.21.1
pydantic==2.10.3
scikit-learn==1.5.2
xgboost==2.1.3
# Real LLM-as-Judge (Phase 1): Groq's free-tier API, JSON mode.
# (groq<0.13 calls its underlying httpx.Client with a `proxies=` kwarg that
# httpx>=0.28 removed — pinned to a version built against modern httpx.)
groq==0.37.1
