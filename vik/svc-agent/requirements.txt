fastapi==0.115.6
uvicorn[standard]==0.32.1
prometheus-client==0.21.1
pydantic==2.10.3
langchain==0.3.7
langchain-community==0.3.7
langchain-core==0.3.17
# Real reasoning (Phase 1): Groq's free-tier API (primary), via langchain's
# tool-calling agent instead of the earlier FakeListLLM + hand-rolled ReAct
# parsing. Pinned directly (not just pulled in transitively by
# langchain-groq) for reproducibility.
langchain-groq==0.2.1
groq==0.37.1
# Local fallback provider (no API key/quota, ever) — see agent.py's
# multi-provider chain and infra/docker-compose.yml's `ollama` service.
langchain-ollama==0.2.0
# For calling svc-rag (retrieval) and svc-guard (groundedness judge).
httpx==0.27.2
